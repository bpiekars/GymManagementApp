README.TXT for Gym Management App "Group 11"

This is a C# Windows Form Application that requires the Windows version of Visual Studio in order to run properly.
In order to launch the program, you can either run it directly within Visual Studio (recommended) or open the .exe file directly by
opening the ToyProject folder then opening the bin folder then the Debug folder and launching GymManagementApp.exe.